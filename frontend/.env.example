# Base URL of the Flask backend. Point this at your local server while
# developing, and at the deployed Render backend URL once it's live.
#
# Local dev:
# VITE_API_BASE_URL=http://127.0.0.1:5000
#
# Deployed (Render):
VITE_API_BASE_URL=https://middleman-nile.onrender.com
